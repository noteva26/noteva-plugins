# Noteva 插件集市 - 提交指南

本目录用于第三方插件开发者提交插件索引，让您的插件出现在 Noteva 官方插件集市中。

## 📋 提交方式

在 `store/` 目录下创建以您的插件 ID 命名的文件夹，并在其中放置 `plugin.json` 文件。

**目录结构示例：**
```
store/
  your-plugin-id/
    plugin.json
```

## 📝 plugin.json 格式说明

```json
{
  "id": "your-plugin-id",
  "name": "插件显示名称",
  "version": "1.0.0",
  "description": "插件功能描述",
  "author": "作者名称",
  "homepage": "https://github.com/your-username/your-plugin-repo",
  "license": "MIT",
  "requires": {
    "noteva": ">=0.0.8"
  },
  "hooks": {
    "frontend": ["content_render"],
    "editor": ["toolbar"]
  },
  "shortcodes": ["your-shortcode"],
  "permissions": ["read_comments"],
  "settings": true,
  "database": false
}
```

## 🔑 必填字段

| 字段 | 说明 | 示例 |
|------|------|------|
| `id` | 插件唯一标识符（建议使用小写字母和连字符） | `"my-awesome-plugin"` |
| `name` | 插件显示名称 | `"我的超棒插件"` |
| `version` | 插件版本号（遵循语义化版本） | `"1.0.0"` |
| `description` | 插件功能描述 | `"为博客添加酷炫功能"` |
| `author` | 作者名称 | `"Your Name"` |
| `homepage` | **插件仓库地址（必填）** | `"https://github.com/you/repo"` |
| `requires.noteva` | **支持的 Noteva 版本（必填）** | `">=0.0.8"` |

## ⚠️ 重要提示

### 关于 `homepage` 字段
- **必须填写**您的插件 GitHub 仓库地址
- 用户将从此仓库的 Releases 下载插件
- 请确保仓库中有可用的 Release 版本

### 关于 `requires.noteva` 字段
- **必须填写**插件支持的 Noteva 最低版本
- 支持的格式：
  - `">=0.0.8"` - 大于等于 0.0.8
  - `"<=0.1.0"` - 小于等于 0.1.0
  - `">0.0.7"` - 大于 0.0.7
  - `"<0.2.0"` - 小于 0.2.0
  - `"0.0.8"` - 精确版本
- ⚠️ **如果用户的 Noteva 版本不满足要求，插件将无法安装和启用**

## 📦 发布流程

1. **开发插件**：在您自己的 GitHub 仓库中开发插件
2. **创建 Release**：在您的仓库中创建 Release，上传插件压缩包（.zip 或 .tar.gz）
3. **提交索引**：Fork 本仓库，在 `store/` 目录下添加您的 `plugin.json`
4. **提交 PR**：提交 Pull Request，等待审核

## 📚 完整示例

```json
{
  "id": "hide-until-reply",
  "name": "回复可见",
  "version": "1.0.0",
  "description": "设置部分内容需要回复后才能查看",
  "author": "Noteva Team",
  "homepage": "https://github.com/noteva26/noteva-plugins",
  "license": "MIT",
  "requires": {
    "noteva": ">=0.0.8"
  },
  "hooks": {
    "frontend": ["content_render", "comment_after_create"],
    "editor": ["toolbar"]
  },
  "shortcodes": ["hide-until-reply"],
  "permissions": ["read_comments", "read_users"],
  "settings": true,
  "database": false
}
```

## 🤝 贡献

欢迎提交您的插件到 Noteva 官方集市！如有问题，请在 Issues 中提出。

---

**注意**：官方插件直接放在仓库根目录，第三方插件索引放在 `store/` 目录。
